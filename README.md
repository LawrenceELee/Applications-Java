Repositary to host interesting applications/clients using data structures.

* TopM using PriorityQueue/Heap
* Finding duplicates using sorting and traversing sorted array. (palindrome, anagram).
* ...
